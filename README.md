# Bluetooth Device Notify

Show desktop notification when device is connected/disconnected.

If you use systemd enabled desktop environment you can enable autostart by putting the `BluetoothDeviceNotify.service` file into `~/.config/systemd/user` folder.
